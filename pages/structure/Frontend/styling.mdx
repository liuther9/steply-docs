## Styling

Use ```[componentName].module.scss``` for styling and place it inside ```[ComponentName]```

Global styles are inside ```app/global.css```

You can place shared colors etc in ```src/styles/_variables.scss```